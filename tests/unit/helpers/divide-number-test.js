import {
  divideNumber
} from 'mockup-test/helpers/divide-number';

module('DivideNumberHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = divideNumber(42);
  ok(result);
});
